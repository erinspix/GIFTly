const connection = require ('..config/db');
const { Item,User } = require ('../models');
const { getRandomItem, getRandomUser } = require('./helper');

de.on(error', (err) => err);

db.once('open',async () => {
    comnsole.log('connected');
    // Delete the collections if they exist
    let itemCheck
}
